{
  "Servers": {
    "1": {
      "Name": "My Postgres DB",
      "Group": "Servers",
      "Host": "my_postgres_db",
      "Port": 5432,
      "MaintenanceDB": "${POSTGRES_DB}",
      "Username": "${POSTGRES_USER}",
      "Password": "${POSTGRES_PASSWORD}",
      "SSLMode": "prefer",
      "Favorite": true
    }
  }
}
